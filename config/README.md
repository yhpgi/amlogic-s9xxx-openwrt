# Configs

This repository keeps only ImageBuilder configuration.
Package selection uses the OpenWrt 25 default package list.

- `config/imagebuilder/imagebuilder.sh` runs the ImageBuilder build.
- `config/imagebuilder/files` provides overlay files copied into the rootfs.
